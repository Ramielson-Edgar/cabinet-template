  // windowWidth = window.innerWidth;
  // containerWidth = document.querySelector('.horizontal-container').offsetWidth / 2;
  // horLength = document.querySelector(".list-features.horizontal-scroll").scrollWidth + containerWidth;
  // sectionHeader = document.querySelector('.section-header.top-choose').offsetWidth / 2
  // distFromTop = document.querySelector(".top-choose").offsetTop + sectionHeader;
  // scrollDistance = distFromTop + horLength + windowWidth / 2 - windowWidth;
  // setSectionHeight = document.querySelector(".top-choose").style.height = horLength + containerWidth + "px";
  
    // if (!isMobile()) {

    //     for (let i = 0; i < sticky.length; i++) {
    //         transform(sticky[i]);
    //     }
    // }


    // function transform(section) {
    //     const offsetTop = section.parentElement.offsetTop;
    //     const scrollSection = document.querySelector('.list-features.horizontal-scroll');
    //     const maxScroll = scrollSection.scrollWidth - scrollSection.offsetWidth;
    //     let percentage = ((window.scrollY - offsetTop) / window.innerHeight) * 100;
    //     // percentage = Math.min(Math.max(percentage, maxScroll), 70);
    //     // scrollSection.style.transform = `translate3d(${-percentage}vw, 0, 0)`;

    //     if(window.innerWidth >= 1450) {
    //         percentage = Math.min(Math.max(percentage, maxScroll), 40);
    //         scrollSection.style.transform = `translate3d(${-percentage}vw, 0, 0)`;
    //     }


    //     if(window.innerWidth >= 992) {
    //         percentage = Math.min(Math.max(percentage, maxScroll), 70);
    //         scrollSection.style.transform = `translate3d(${-percentage}vw, 0, 0)`;
    //     }


    //     if(window.innerWidth <= 991) {
    //         percentage = Math.min(Math.max(percentage, maxScroll), 200);
    //         scrollSection.style.transform = `translate3d(${-percentage}vw, 0, 0)`;
    //     }


    //     if(window.innerWidth <= 767) {
    //         percentage = Math.min(Math.max(percentage, maxScroll), 200);
    //         scrollSection.style.transform = `translate3d(${-percentage}vw, 0, 0)`;
    //     }



    // }

    // function isMobile() {
    //     return window.matchMedia("(max-width: 575px)").matches;
    // }


    // function transform(section) {
    //     const offsetTop = section.parentElement.offsetTop;
    //     const scrollSection = document.querySelector('.list-features.horizontal-scroll');
    //     const maxScroll = scrollSection.scrollWidth - scrollSection.offsetWidth;
    //     let percentage = ((window.scrollY - offsetTop) / window.innerHeight) * 100;
    
    //     // Calculate maxPercentage based on the ratio of maxScroll to scrollSection.scrollWidth
    //     const maxPercentage = (maxScroll / scrollSection.scrollWidth) * 100;
    
    //     // Ensure percentage is within bounds
    //     percentage = Math.min(Math.max(percentage, 0), maxPercentage);
    
    //     scrollSection.style.transform = `translate3d(${-percentage}vw, 0, 0)`;
    // }
    
    // function isMobile() {
    //     return window.matchMedia("(max-width: 575px)").matches;
    // }

